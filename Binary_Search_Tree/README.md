# Task 4: Binary Search Tree (бинарное дерево поиска)

**Реализация бинарного дерева поиска с полным набором операций над узлами.**  
Дерево поиска поддерживает операции добавления, удаления, поиска элементов и вывода структуры дерева с соблюдением свойств BST.

## 📋 Требования

Реализовать бинарное дерево поиска с фиксированными операциями:
- `add K V` - добавление элемента с ключом K и значением V
- `set K V` - изменение значения по ключу K
- `delete K` - удаление узла с ключом K
- `search K` - поиск значения по ключу K
- `min` - поиск узла с минимальным ключом
- `max` - поиск узла с максимальным ключом
- `print` - вывод дерева по уровням

## 🏗️ Архитектура решения

### Файловая структура:
```
Binary_Search_Tree/
├── binary_search_tree.hpp    # Заголовочный файл с классом Binary_tree и его реализацией
├── prog.cc                   # Основная программа с обработкой ввода/вывода
├── oneFileProg.cc            # Однофайловая версия
└── README.md                 # Описание задачи
```

### Ключевые особенности:
- **Инкапсулированная реализация** структуры данных, независимой от форматов ввода/вывода
- **Левостороння реализация** - при симметричных операциях приоритет отдаётся левой стороне
- **Динамическое управление памятью** - узлы создаются и удаляются по необходимости
- **Корректное удаление** узла с двумя потомками через замену максимумом левого поддерева
- **Целостность указателей** - каждый узел содержит ссылку на родителя для корректного вывода
- **Обработка ошибок** - корректная работа с пустым деревом и невалидными ключами

## 🛠 Компиляция и запуск

**С раздельной компиляцией:**
```
cd Binary_Search_Tree
g++ -std=c++17 binary_search_tree.cc prog.cc -o bst
./bst
```

**Однофайловая версия:**
```
cd Binary_Search_Tree
g++ -std=c++17 oneFileProg.cc -o oneFileProg
./oneFileProg
```

## 📝 Формат ввода/вывода

### Входные данные:
```
add K V          # Добавить элемент с ключом K и значением V
set K V          # Изменить значение по ключу K
delete K         # Удалить узел с ключом K
search K         # Найти значение по ключу K
min              # Найти узел с минимальным ключом
max              # Найти узел с максимальным ключом
print            # Вывести дерево по уровням
```

### Выходные данные:
- `search K`: `"1 V"` (найден) или `"0"` (не найден)
- `min/max`: `"K V"` (ключ и значение) или `"error"` (дерево пусто)
- `add/set/delete`: без вывода при успехе, `"error"` при неудаче
- `print`: дерево по уровням или `"_"` (пустое дерево)

### Формат вывода print:
```
[K V]                        # Корень (уровень 0)
[K1 V1 P1] [K2 V2 P2] ...  # Уровень 1 (K1, K2 - ключи, P1, P2 - ключи родителей)
[K3 V3 P3] _ [K4 V4 P4] ... # Уровень 2 (пустые позиции обозначены "_")
...
```

## 🧪 Примеры работы

### Пример 1: Базовые операции
```
# Ввод:
add 8 10
add 4 14
add 7 15
add 9 11
add 3 13
add 5 16
add 88 1
add 11 2
add 100 18
print
search 7
search 99
delete 7
print


# Вывод:
[8 10]
[4 14 8] [9 11 8]
[3 13 4] [7 15 4] _ [88 1 9]
_ _ [5 16 7] _ _ _ [11 2 88] [100 18 88]
1 15
0
[8 10]
[4 14 8] [9 11 8]
[3 13 4] _ _ [88 1 9]
_ _ [5 16 4] _ _ _ [11 2 88] [100 18 88]
```

### Пример 2: Операции min/max и удаление
```
# Ввод:
add 5 a
add 3 b
add 7 c
add 1 d
add 9 e
min
max
delete 3
print
delete 1
min


# Вывод:
1 d
9 e
[5 a]
[1 d 5] [7 c 5]
_ _ _ [9 e 7]
5 a
```

### Пример 3: Ошибки
```
# Ввод:
add 10 x
add 10 y
set 20 z
delete 99
search 10
add 10 y
print


# Вывод:
error
error
error
1 x
error
[10 x]
```

## 🔍 Принцип работы

### Структура узла:
```
struct Node {
    Key key;               // Ключ узла
    Val value;             // Значение узла
    Node* left_child;      // Указатель на левого потомка
    Node* right_child;     // Указатель на правого потомка
    Node* parent;          // Указатель на родителя
}
```

### Логика операций:
- **add**: вставка нового узла, если ключ не существует; сохраняет левостороннюю ориентацию
- **set**: поиск узла и обновление значения; ошибка если ключ не найден
- **delete**: удаление узла с корректной перестройкой дерева
  - Узел без потомков: просто удаляется
  - Узел с одним потомком: потомок занимает его место
  - Узел с двумя потомками: заменяется максимумом левого поддерева
- **search**: бинарный поиск в дереве
- **min/max**: поиск по левой/правой цепочке
- **print**: обход по уровням (BFS) с вывод родителя каждого узла

## ⚠️ Обработка ошибок

Программа обрабатывает следующие ситуации:
- **Дублирование ключа** при добавлении (команда `add`)
- **Отсутствие ключа** при изменении или удалении (команды `set`, `delete`)
- **Пустое дерево** при поиске min/max
- **Некорректные команды** или аргументы
- **Пустые строки** в входном потоке (игнорируются)

## 🎯 Особенности реализации

- **Инкапсуляция** - класс Dequeue полностью независим от форматирования ввода/вывода
- **Динамическое управление памятью** - корректное выделение и освобождение узлов
- **Целостность структуры** - сохранение инвариантов BST при всех операциях
- **Указатели на родителя** - необходимы для корректного удаления и вывода
- **Обход по уровням** - использование очереди (queue) для обхода BFS при выводе
- **Левостороння ориентация** - при необходимости выбора между левым и правым, приоритет левому
- **Эффективность операций** - O(log n) средний случай для сбалансированного дерева
- **Ограничения**: Время: 1 секунда, Память: 64 МБ, Ключ: 64-битное целое число
